export async function getCalendarEvents(_query: string): Promise<string> {
  // Google Calendar integration placeholder.
  // Requires GOOGLE_CALENDAR_CREDENTIALS to be configured.
  return "Calendar integration not configured. Set GOOGLE_CALENDAR_CREDENTIALS in .env";
}
